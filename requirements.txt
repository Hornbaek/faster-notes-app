faster-whisper>=1.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.7
httpx>=0.25.0
cryptography>=42.0.0
pystray>=0.19.5
Pillow>=10.0.0
zeroconf>=0.130.0
PyYAML>=6.0          # skill files use YAML frontmatter

# Build-only (not needed at runtime):
#   pyinstaller>=6.0
